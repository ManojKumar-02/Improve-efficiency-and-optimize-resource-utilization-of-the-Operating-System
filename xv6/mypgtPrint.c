#include "types.h"
#include "user.h"
#include "date.h"

// int arrGlobal[10000] ; // For solving the global array part

int main()
{

   // The following code is for solving the local array part
   // int arrlocal[10000] ;
   // for(int i=0;i<10000;i++) arrlocal[i] = 0 ;
   // int sum = 0;
   // for(int i=0;i<10000;i++) sum += arrlocal[i] ;
   // sum += sum + 9 ;
    
    
    mypgtPrint();    
    exit();
}





